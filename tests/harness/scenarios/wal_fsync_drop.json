{
  "name": "wal_fsync_drop",
  "description": "Simulate fsync lies - durability failure simulation",
  "db_path": "$TMPDIR/decentdb_wal_fsync_drop.ddb",
  "sql": "CREATE TABLE test (id INT PRIMARY KEY, data TEXT); INSERT INTO test VALUES (1, 'fsync test')",
  "failpoint": {
    "label": "wal_fsync",
    "kind": "drop"
  },
  "verify": {
    "expect_crash": false,
    "post_sql": "SELECT * FROM test WHERE id = 1",
    "expect_rows": ["1|fsync test"]
  }
}
