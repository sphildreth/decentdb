{
  "name": "wal_commit_fail",
  "description": "Crash during commit frame write - transaction should not be visible",
  "db_path": "$TMPDIR/decentdb_wal_commit_fail.db",
  "sql": "CREATE TABLE test (id INT PRIMARY KEY, data TEXT); INSERT INTO test VALUES (1, 'committed data')",
  "failpoint": {
    "label": "wal_commit",
    "kind": "error"
  },
  "verify": {
    "expect_crash": true,
    "post_crash_sql": "SELECT * FROM test WHERE id = 1",
    "expect_rows": []
  }
}
