{
  "name": "wal_commit_fail",
  "description": "Fail a WAL frame write - transaction should not be visible",
  "db_path": "$TMPDIR/decentdb_wal_commit_fail.ddb",
  "setup_sql": "CREATE TABLE test (id INT PRIMARY KEY, data TEXT)",
  "sql": "INSERT INTO test VALUES (1, 'committed data')",
  "failpoint": {
    "label": "wal_write_frame",
    "kind": "error"
  },
  "verify": {
    "expect_crash": true,
    "post_crash_sql": "SELECT * FROM test WHERE id = 1",
    "expect_rows": []
  }
}
