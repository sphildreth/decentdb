{
  "name": "multi_txn_crash",
  "description": "Multiple transactions - committed should survive, uncommitted should not",
  "db_path": "$TMPDIR/decentdb_multi_txn_crash.ddb",
  "setup_sql": "CREATE TABLE test (id INT PRIMARY KEY, status TEXT); INSERT INTO test VALUES (1, 'committed')",
  "sql": "INSERT INTO test VALUES (2, 'uncommitted')",
  "failpoint": {
    "label": "wal_write_frame",
    "kind": "error",
    "count": 1
  },
  "verify": {
    "expect_crash": true,
    "post_crash_sql": "SELECT id, status FROM test ORDER BY id",
    "expect_rows": ["1|committed"]
  }
}
