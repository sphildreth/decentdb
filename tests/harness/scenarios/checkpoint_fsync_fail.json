{
  "name": "checkpoint_fsync_fail",
  "description": "Fsync failure during checkpoint - interrupted checkpoint handling",
  "db_path": "$TMPDIR/decentdb_checkpoint_fsync_fail.ddb",
  "setup_sql": "CREATE TABLE test (id INT PRIMARY KEY, data TEXT); INSERT INTO test VALUES (1, 'checkpoint test')",
  "checkpoint": true,
  "failpoint": {
    "label": "checkpoint_fsync",
    "kind": "error"
  },
  "verify": {
    "expect_crash": true,
    "post_crash_sql": "SELECT * FROM test WHERE id = 1",
    "expect_rows": ["1|checkpoint test"]
  }
}
