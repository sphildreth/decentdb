{
  "name": "freelist_free_crash",
  "description": "Crash during WAL fsync after committed insert - committed data should survive recovery (ADR-0057)",
  "db_path": "$TMPDIR/decentdb_freelist_free_crash.ddb",
  "setup_sql": "CREATE TABLE test (id INT PRIMARY KEY, data TEXT); INSERT INTO test VALUES (1, 'first'); INSERT INTO test VALUES (2, 'second')",
  "sql": "INSERT INTO test VALUES (3, 'third')",
  "failpoint": {
    "label": "wal_fsync",
    "kind": "error"
  },
  "verify": {
    "expect_crash": true,
    "post_crash_sql": "SELECT id, data FROM test ORDER BY id",
    "expect_rows": ["1|first", "2|second", "3|third"]
  }
}
