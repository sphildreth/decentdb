{
  "name": "torn_write_payload",
  "description": "Write full header but partial payload - recovery should detect",
  "db_path": "$TMPDIR/decentdb_torn_write_payload.ddb",
  "setup_sql": "CREATE TABLE test (id INT PRIMARY KEY, data TEXT)",
  "sql": "INSERT INTO test VALUES (1, 'large payload to exceed partial write')",
  "failpoint": {
    "label": "wal_write_frame",
    "kind": "partial",
    "bytes": 64
  },
  "verify": {
    "expect_crash": true,
    "post_crash_sql": "SELECT * FROM test",
    "expect_rows": []
  }
}
