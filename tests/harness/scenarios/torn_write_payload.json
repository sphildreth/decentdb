{
  "name": "torn_write_payload",
  "description": "Write full header but partial payload - recovery should detect",
  "db_path": "$TMPDIR/decentdb_torn_write_payload.ddb",
  "sql": "CREATE TABLE test (id INT PRIMARY KEY, data BLOB); INSERT INTO test VALUES (1, 'large blob data that exceeds partial write')",
  "failpoint": {
    "label": "wal_frame",
    "kind": "partial",
    "bytes": 64
  },
  "verify": {
    "expect_crash": true,
    "post_crash_sql": "SELECT * FROM test",
    "expect_rows": []
  }
}
